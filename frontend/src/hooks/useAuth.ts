/**
 * Convenience re-export — delegates to AuthContext.
 * Use useAuthContext() directly for full API (login, logout, loading).
 */
export { useAuthContext as useAuth } from "../context/AuthContext";
